{
  "name": "ea-seminari-3-mongodb",
  "version": "1.0.0",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/main.js",
    "dev": "tsx src/main.ts",
    "lint": "npx oxlint@latest",
    "check": "tsc --noEmit",
    "validate": "npm run lint && npm run check"
  },
  "dependencies": {
    "mongoose": "^9.2.1",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
